// Trading → Strategies → Scalping · Order Book — placeholder, not yet implemented.
export default function Page() {
  return (
    <div className="flex flex-col items-center justify-center h-[60vh] gap-2 px-6 text-center">
      <p className="text-lg font-semibold">Scalping · Order Book</p>
      <p className="text-sm text-muted-foreground max-w-md">Order-book driven scalping — depth imbalance, queue dynamics and microstructure signals. Requires live order-book data (not currently imported).</p>
      <p className="text-xs text-muted-foreground/60 mt-2">Not built yet.</p>
    </div>
  )
}
